# Graphs Assignment 2

Python project for problem 4 from practical work no. 2.

Included:
- an undirected graph ADT based on the lab 1 interface
- connected components found with breadth-first search
- sample graphs, tests, and manual execution for the assigned problem

Run the console:

```bash
cd pythonProject
python -m src.main
```

Run the tests:

```bash
cd pythonProject
python -m unittest discover -s tests
```
